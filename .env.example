# Example only. Do not put real credentials in this file.

APP_ENV=development
PUBLIC_APP_NAME=MUSE-AI
PUBLIC_APP_URL=http://localhost:3000

# Optional service placeholders
API_BASE_URL=http://localhost:54321
AI_PROVIDER=example-provider
AI_MODEL=example-model

# Never commit real secret values.
SERVER_SECRET_KEY=replace-with-local-dev-value
